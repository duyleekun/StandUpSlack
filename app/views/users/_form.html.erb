<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :real_name %><br>
    <%= f.text_area :real_name %>
  </div>
  <div class="field">
    <%= f.label :team_id %><br>
    <%= f.number_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :slack_id %><br>
    <%= f.text_field :slack_id %>
  </div>

    <div class="field">
        <%= f.label :daily_prompt_active %><br>
        <%= f.check_box :daily_prompt_active %>
    </div>

    <div class="field">
        <%= f.label :daily_digest_enabled %><br>
        <%= f.check_box :daily_digest_enabled %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
